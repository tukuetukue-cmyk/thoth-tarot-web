# Thoth Tarot Project Context & Rules

このファイルは、AI（ハル）が過去の文脈や重要な決定事項を忘れて頓珍漢な提案をすることを防ぐための「外部記憶（絶対のカンペ）」です。
**ハルは、何か新しい提案や実装計画を作成する前に、必ずこのファイルを読んで最新の前提条件を頭に入れること。**

## 1. フロントエンドの前提条件（絶対順守）
- **WebとAPIの分離**: 本リポジトリ(`thoth-tarot-web`)はフロントエンド専用です。バックエンドロジック(Gemini連携や太陽星座計算など)は別リポジトリ(`thoth-tarot-api`)に存在します。
- **UIの制約**: 画像保存機能はなくプレーンテキストコピーのみ。
- **デザインの維持**: ダークモード、グラスモーフィズム、SVGインタラクティブUIなどのプレミアムなデザインを崩さないこと。

## 2. アプリの仕様と制約
- **鑑定回数の制限 (クライアント側実装)**: ユーザー単位ではなく、ブラウザの `localStorage` 単位で「1日3回」までの制限がかかっています。
- **開発時のバイパス**: ローカル環境（localhost / 127.0.0.1）でのテスト時は、この回数制限のみが無効化されます。（APIへの接続先が localhost になるわけではありません。通常のリーディング等はローカルでも本番APIに接続します）
- **3枚引き機能**: コード上には3枚引きのロジックが存在しますが、通常のUI上では非表示になっています。
- **force_card**: クエリーパラメーター `force_card` を使用した特定カードの強制表示は、本番環境でも利用可能な既知の仕様です。
- **多言語対応 (JP/EN)**: 完全ではありません。動的な結果や一部の画面では切り替えが即時反映されない場合があります。

## 3. デザインと世界観のルール・既知の不整合
- **トーン＆マナー**: トート・タロットの厳格で深淵な世界観を崩さないこと。エラーメッセージも世界観に合わせた表現にすること。
- **商用・投げ銭リンクの扱い (既知の不整合)**: Ko-fiやMOSHなどのリンクについて、「永久に削除する」という方針と、「一時的にHTMLコメントアウトで非表示にする」という記述が衝突しています。どちらが正しいかは推測せず、エクツに確認してください。
- **絵文字の扱い (既知の不整合)**: ドキュメント上はサイト全体で絵文字(🔮や✨など)を禁止していますが、実装内には可視の絵文字が存在する不整合があります。これも仕様未確定として扱います。

## 4. セキュリティ上の要対応事項 (既知の問題)
以下のセキュリティリスクは現状存在しますが、勝手にコードを修正しないでください。
- `innerHTML` にユーザー入力やAI出力を入れている箇所の DOM XSS リスク。
- 鑑定回数制限がクライアント側 (localStorage) のみでの制御となっている。
- API呼び出しに認証（APIキー等）がない。
- CSP / SRI / セキュリティヘッダーが未整備。
- `localStorage` に名前・生年月日・相談内容・AI結果などの個人情報が保存されている。
- APIリクエスト時の timeout / `AbortController` / 多重送信対策がない。

## 5. ハル（AI）の行動規範
- **推測で動かない**: わからないことは素直に「わからない」とエクツに聞くこと。
- **軽率な変更をしない**: どんな些細な変更でも、必ず実装計画を提示し合意を得ること。
- **質と正確さを最優先**: スピードよりも正確さと世界観の維持を優先すること。
- **最新コードの確認**: 作業前には必ず最新のコードや Git 履歴を確認すること。
- **越権行為の禁止**: エクツの指示したタスク以外は絶対に同時に行わず、既存コードの削除などは実行前に必ず確認と合意を取ること。

## 6. レポートの常時更新と同期
- サイトの改善・修正プロセスは常に `project_log.md` に詳細を記録する。
- 新しいセッションが開始された際は必ず `project_log.md` を確認すること。
